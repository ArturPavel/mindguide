>[!WARNING]
>The Python code is not mine; only the JavaScript and Electron parts are.
>This is also a project where I tried integrating AI code into my application. This is the only project I submitted where I did this, as I found it to be quite inconvenient and inefficient for system design. Hence, from then on, I write all the code myself and use AI only as a helper.

# How to build
<code>cd</code> into the main folder and execute <code>npm run build</code>
> [!IMPORTANT]  
> Dependencies: Python 3.10 or newer, pip, Node.js, npm

# How to run
After the application has been built <code>cd</code> into the main folder and execute <code>npm run start</code>
